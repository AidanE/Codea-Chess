piece = class()

function piece:init(x,y)
    -- you can accept and set parameters here
    self.x = x
    self.y = y
    self.selected = false
end

function piece:draw()
    -- Codea does not automatically call this method
end

function piece:touched(touch)
    -- Codea does not automatically call this method
end
