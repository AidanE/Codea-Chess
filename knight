knight = class(piece)

function knight:init(x)
    -- you can accept and set parameters here
    piece.init(self,x,y)
end

function knight:draw()
    -- Codea does not automatically call this method
end

function knight:touched(touch)
    -- Codea does not automatically call this method
end
