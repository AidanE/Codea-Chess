bishop = class(piece)

function bishop:init(x)
    -- you can accept and set parameters here
    self.x = x
end

function bishop:draw()
    -- Codea does not automatically call this method
end

function bishop:touched(touch)
    -- Codea does not automatically call this method
end
