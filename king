king = class(piece)

function king:init(x)
    -- you can accept and set parameters here
    self.x = x
end

function king:draw()
    -- Codea does not automatically call this method
end

function king:touched(touch)
    -- Codea does not automatically call this method
end
