pawn = class(piece)

function pawn:init(x)
    -- you can accept and set parameters here
    piece.init(self,x,y)
end

function pawn:draw()
    -- Codea does not automatically call this method
end

function pawn:touched(touch)
    -- Codea does not automatically call this method
end
